# 犯错档案

记录仿真开发过程中出现过的错误、原因、修复方式和后续预防措施。

## 记录格式

### YYYY-MM-DD：错误标题

- **现象**：
- **原因**：
- **修复**：
- **预防**：

## 已记录错误

### 2026-07-27：WSL 隔离构建的网络初始化与跨 Shell 引号处理失败

- **现象**：首次验证假定 `/home/car/.config/smartcar/ros_network.sh` 存在，但当前 WSL 中没有该文件；随后把动态 IP 提取逻辑嵌入 PowerShell、WSL 和 Bash 的多层命令时，又先后出现 `awk` 解析错误、`cut` 参数被错误拆分以及命令提前退出。通过 `env` 传入的变量还会被 `bash -lc` 的登录初始化清空。
- **原因**：没有先检查共享网络配置文件是否存在，并在一条跨 Shell 命令里同时完成地址发现、字符串解析和构建，导致不同 Shell 的变量展开、登录初始化和引号规则互相干扰。
- **修复**：先用独立的只读 WSL 命令获取当前地址；构建与测试在明确的临时目录内完成；需要 ROS 网络的命令则在加载 ROS 环境后，把已获取的地址直接写入当前 Bash 命令并导出 `ROS_IP` 与 `ROS_MASTER_URI`。
- **预防**：执行 WSL 验证前先检查网络配置文件；把动态地址发现与实际构建拆成两个步骤；网络变量必须在最后一次 `source` 之后设置；跨 PowerShell/WSL/Bash 调用时避免嵌套文本解析、仅依赖继承环境变量和脆弱的多层引号。

### 2026-07-28：车端部署校验路径、远程引号、权限与 Catkin 白名单错误

- **现象**：首次 SHA-256 校验在远程执行 `cd ~/ucar_ws` 后仍使用 `ucar_ws/src/...`，导致 27 个已上传文件全部误报不存在；随后 PowerShell 抢先解释了发往远程 Bash 的 `$(ip ...)`，构建在 `catkin_make` 前退出；修正引号后，`libcym_planner.so` 已编译，但车端缓存白名单未包含新包 `ucar_2026`，`--pkg ucar_2026` 因没有构建目录而失败；Windows `scp` 后两个脚本也未保留预期可执行位。
- **原因**：远程校验基准目录与相对路径重复；误以为反斜杠能在 PowerShell 中转义 `$()`；没有先检查 Catkin 配置输出中的 `CATKIN_WHITELIST_PACKAGES`；仓库索引和 Windows 文件系统不能提供可直接依赖的 Linux 执行权限。
- **修复**：校验改为从小车家目录执行，并确认 27 个文件哈希全部匹配；远程命令改用单引号包裹的 Bash 脚本和本次已确认的显式网络地址；白名单保留 `cym_planner`、`jie_ware`、`yolo2025` 并加入 `ucar_2026` 后重新构建；两个车端脚本显式设为 `0755`；最终构建及 14 项测试全部通过。收尾时还恢复了 WSL 的当前 `192.168.8.199/24` 地址，并停止一个仅绑定回环地址、没有业务节点的旧 Master。
- **预防**：校验路径以 `ucar_ws/` 开头时固定从 `~` 执行；将网络发现和远程构建拆成独立步骤；跨 PowerShell/SSH/Bash 时不嵌套本地可解释的命令替换；构建前先核对 Catkin 白名单；Windows 上传脚本后执行 `stat` 并显式设置 `0755`；结束前检查 WSL 与小车的 ROS 进程、监听地址和实际业务节点。

### 2026-07-28：Windows CRLF 破坏车端 Python shebang

- **现象**：首次真机启动时 `navigation_scan_relay` 持续以退出码 127 重启，日志为 `/usr/bin/env: ‘python2\r’: No such file or directory`；由于 `/scan_raw -> /scan` 中继未启动，`lidar_loc` 无法建立 `map` TF，`move_base` 一直等待 `base_link -> map`。
- **原因**：Git 索引中的脚本为 LF，但仓库没有 `.gitattributes`；Windows 工作树在检出时转换为 CRLF，`scp` 又逐字节复制到 Ubuntu 18.04，使 shebang 以 `0d 0a` 结束。Python 2 存在，脚本文本和解释器路径本身均正确。
- **修复**：立即发布零速度并停止失败的车端 launch；新增 `.gitattributes` 强制 `*.py`、`*.sh` 使用 LF，规范化 `navigation_scan_relay.py` 与 `stop_2026_task.sh` 后重新上传并设为 `0755`。车端首行确认仅以 `0a` 结束，完整重启后 relay、定位、`move_base` 与本地 RViz 均正常连接。
- **预防**：从 Windows 部署脚本前检查 `git ls-files --eol` 必须显示 `w/lf`；上传后用 `od -An -tx1` 检查 shebang 不含 `0d`；真实启动测试必须等待 relay ready，再执行有限里程计、两条 TF 和扫描频率安全门检查。

### 2026-07-28：完整任务的 action 等待短于真机规划器初始化时间

- **现象**：完整任务第一次启动时，二维码和生产路线配置均加载成功，但 `navigation_2026` 在 15 秒后报告 `move_base action server was not available`；CymPlanner 约 33 秒后才完成初始化，所以没有发送启动目标。停止 launch 时，一个已经排队的激光回调还尝试向关闭的话题发布，产生 `publish() to a closed topic`。
- **原因**：任务节点固定只等待 action server 15 秒，没有复用已有的 90 秒启动就绪预算；订阅回调也没有覆盖 roslaunch 关闭发布者与最后一帧回调重叠的情况。
- **修复**：先发布零速度并停止本次无运动任务；action server 等待改用 `startup_goal_ready_timeout`，激光回调在 ROS 关闭时直接返回并忽略关闭阶段的发布异常。
- **预防**：完整任务的首个自动目标必须使用覆盖最慢真机插件初始化的统一启动预算；真机验证需从冷启动计时到 action server ready，不能只以节点进程存在作为就绪条件；关闭时的高频传感器回调必须容忍发布者先关闭。

### 2026-07-28：真机错误启用了仿真前端未使用的车体投影模式

- **现象**：修复启动等待后，定位和全局规划连续 5 次通过并成功发送启动目标；CymPlanner 报告激光点 `(0.053, 0.182) m` 进入预测车体足迹，action 状态为 4，任务跳过二维码和生产路线。
- **原因**：拉取并部署的真机参数把 `navigation_mode` 设成了 `laser_avoidance`；对照仿真仓库后确认，仿真前端 `gazebo_nav.launch` 和 `task3_prepare.launch` 默认均为 `main_legacy`。第一次改值后又发现 YAML 根键 `cym_planner/CymPlanner` 与插件实际读取的 `CymPlanner` 私有命名空间不一致，整份 YAML 原先都被插件忽略，日志中的 `scan_timeout=0.25` 而非配置值 `0.4` 是直接证据。
- **修复**：先发布零速度并停止任务；将真机配置改为 `navigation_mode: main_legacy`，并把 YAML 根键修正为 `CymPlanner`，使参数落到 `/move_base/CymPlanner/*`。
- **预防**：从仿真移植控制器时必须同时核对 launch 的实际参数覆盖，不能只依据 C++ 可用能力或参数服务器上存在同名键就认为配置已生效；真机启动后必须同时检查 `/move_base/CymPlanner/navigation_mode`、非默认参数值和插件初始化日志。

### 2026-07-28：完整任务冷启动时底盘 Odom/IMU 短暂掉线

- **现象**：修正 `main_legacy` 参数命名空间后的完整任务冷启动中，底盘先报告 `Odom sensor not active any more`、`Imu sensor not active any more`，随后虽自动重新初始化并激活，但连续出现 AHRS/IMU `head_len` 和 CRC 校验错误，TF 一度停滞。将 CP2102 完全绕过 Terminus Hub、直接接入主机根 Hub 后，仍复现 AHRS CRC、帧长错误；`odom -> base_link` 最新时间同时停在约 3 秒前。
- **原因**：早期隔离测试确认过 `usb 1-2` 的 Terminus USB2.0 Hub 整体断开，Hub 下的 CP2102 底盘串口、USB Misc Gadget 和音频设备同时消失并重新枚举，因此原 Hub 链路确实存在故障，但不是全部原因。直连复现时，`lsusb -t` 显示 CP2102 独占主机根 Hub 的 `1-2` 端口，内核只有开机枚举，没有新的 disconnect/reset；这将剩余范围缩小到 CP2102 模块、CP2102 到底盘 MCU 的串口线束/接插件/地线、底盘控制板供电或串口信号完整性，以及驱动未检查单字节读取返回长度所造成的解析失步。XML-RPC、move_base、雷达、相机、扬声器和当前 yolo2025 修改均不是该串口错误的必要条件。
- **修复**：自动目标执行期间立即发布零速度并停止完整任务，准备重启底盘、里程计和导航链路；不得因传感器随后自行恢复而忽略安全门。
- **预防**：每次冷启动除单帧有限值外，还需至少覆盖已出现故障的时间窗口观察 `/odom_raw` 连续频率及底盘日志；出现 sensor inactive、CRC 错误、TF 停滞或内核 Hub disconnect 时必须停车并整链停止。若直连时内核 USB 拓扑稳定但仍有 CRC/帧长错误，不得继续只更换 Hub；应断电后依次替换 CP2102、短 USB 线和 CP2102 到 MCU 的串口线束，并检查接插件、公共地、控制板电源纹波及实际固件波特率。每次只改变一个变量，并以至少 10 分钟的 `base_driver` 静态测试判定。

### 2026-07-28：初学者手工网络命令产生空 ROS_IP

- **现象**：小车终端未先赋值 `MASTER_IP` 时得到 `http://:11311`；重新输入后，较长的 `awk` 命令又在粘贴时与下一条命令串接，最终 `ROS_IP` 为空，roslaunch 报 `invalid ROS_IP (an empty string)`。
- **原因**：快速文档要求初学者逐行复制状态相关且引号复杂的命令，任何漏行或粘贴错位都会污染当前 shell；文档没有提供一个可验证、失败即停止的统一入口。
- **修复**：新增 `start_2026.sh`，只提示输入一次 Master 地址，内部完成环境加载、动态地址计算、空值/回环地址拒绝和 Master 连通检查；快速文档改为一条命令。
- **预防**：面向初学者的日常流程不得暴露多层引号和 `awk`；网络初始化必须封装并在 roslaunch 前打印结果、检查非空地址和 Master 可达性。

### 2026-07-28：一键脚本在加载 Melodic 前过早启用 nounset

- **现象**：车端验证 `start_2026.sh` 的有效地址 `check` 模式时，`/opt/ros/melodic/etc/catkin/profile.d/1.ros_distro.sh` 因 `ROS_DISTRO: unbound variable` 退出。
- **原因**：脚本在 `source /opt/ros/melodic/setup.bash` 前启用了 `set -u`，但 Melodic 自带环境脚本会先读取尚未定义的 `ROS_DISTRO`。
- **修复**：保留启动阶段的 `set -e -o pipefail`，将 `set -u` 移到 ROS 与工作区环境全部加载之后。
- **预防**：封装第三方环境脚本时，严格模式必须考虑其未定义变量行为；至少用接近空白的登录 shell 验证一次。

### 2026-07-28：Python 2 导航节点加载了残留的 Python 3 tf2

- **现象**：`navigation_scan_relay` 即使在新终端中也持续退出并重启，报 `ImportError: dynamic module does not define init function (init_tf2)`；随后 `/scan` 中断，定位无法建立 `map` TF，`move_base` 持续报告 `target_frame map does not exist`。
- **原因**：小车 `ucar_ws/devel` 同时残留 `lib/python2.7/dist-packages` 和 `lib/python3/dist-packages` 两套 Catkin 产物；小车还加载 Python 3 的 `cv_bridge_ws` overlay。Python 2 进程一旦优先命中工作空间的 Python 3 `tf2_py/_tf2.so`，就会因扩展 ABI 不匹配而报 `init_tf2`。
- **修复**：为 Melodic 导航脚本新增专用 Python 2 启动器，剔除所有 Python 3 模块路径并固定执行 `/usr/bin/python2`；一键脚本在 roslaunch 前增加 `rospy/tf/LaserScan` 导入安全门；车端删除已核准的 `devel/lib/python3` 构建输出，并以 `PYTHON_EXECUTABLE=/usr/bin/python2` 重新构建白名单包。验证退出时又发现 relay 的最后一帧回调可能晚于发布者关闭，因此增加 ROS shutdown 检查，只忽略关闭阶段的 `publish() to a closed topic`。
- **预防**：Python 3 的 `cv_bridge` overlay 不得直接决定 Melodic Python 2 节点的模块搜索路径；所有依赖 `tf` 的车端 Python 节点必须经专用启动器运行；构建后检查 `ucar_ws/devel/lib` 不得再出现 `python3*` 目录，并用受污染的 `PYTHONPATH` 做一次反向回归测试。

### 2026-07-28：前后任务阶段错误共用仿真同源巡线控制律

- **现象**：前段虽然切回无车体投影的 `main_legacy`，但真车跑感仍与最初版本不同，转向阻尼、直线减速和弯道速度均发生变化。
- **原因**：移植仿真控制器时，前段 costmap 模式与后段激光车体投影模式共用了同一个控制函数；旧真车的 `angular_kd=0.4`、`linear_x_kd=0.5`、无余弦降速和终点只旋转行为被仿真同源控制律覆盖。
- **修复**：为无投影前段恢复独立的旧真车直接巡线控制，重新接入角度与直线 D 项并使用 `0.25 m` costmap 前视；激光车体投影分支继续保持原有 `main_legacy_*` 参数、余弦降速和 `0.30 m` 投影范围。
- **预防**：任务阶段具有不同碰撞策略或控制手感时必须分离控制状态与参数；任何“恢复前段”的修改都要以差分测试确认后段投影输出未被同一组参数连带改变，且不得把只存在订阅接口误写成任务会自动切换。

### 2026-07-28：未加载 ROS 环境的 ldd 检查误报动态库缺失

- **现象**：车端 `libcym_planner.so` 已构建且此前依赖检查正常，但最后一次裸 SSH 中的 `ldd | grep "not found"` 报告缺少依赖。
- **原因**：该 SSH 命令没有先加载 `/opt/ros/melodic/setup.bash` 和 `~/ucar_ws/devel/setup.bash`，ROS 与工作区动态库路径未进入 `LD_LIBRARY_PATH`。
- **修复**：在同一远程 shell 中依次加载 Melodic 和工作区环境后重新执行 `ldd`，确认没有 `not found`。
- **预防**：对 Catkin 动态库运行 `ldd` 前必须在同一 shell 中完成两次 `source`；不得把未加载 ROS 环境的输出当成构建产物损坏。

### 2026-07-28：把“前视点扩大为车体”误实现成第二套规划器

- **现象**：为了保留后段车体投影，源码增加了 `main_legacy_*` 第二组参数、直接激光订阅、轨迹采样、刹车平滑和另一套控制函数；切换后车辆手感与原始 CymPlanner 不同，参数文件也远超最初的 15 项。
- **原因**：把“碰撞检查点的面积扩大为车体 footprint”误解成“切换到一套激光避障局部规划器”，没有守住控制器、目标点、速度和终点逻辑均不变的边界。
- **修复**：恢复唯一的原始直接巡线 P+D 控制器和唯一参数组；默认只检查前视路径点，`body_projection` 仅把同一位置的检查区域扩大为 local costmap 的真实 footprint；移除第二套参数、直接激光控制和轨迹采样，并恢复 CV Map/Plan ROS 图像。
- **预防**：实现模式前先写出允许变化的最小差分；本模式只允许“碰撞几何：点 → footprint”发生变化。评审时检查 YAML 只能保留约定的 15 项，两个模式必须进入同一控制函数，不能出现模式专属增益或速度参数。

### 2026-07-28：车端 CymPlanner 同时链接两套 OpenCV ABI

- **现象**：Melodic 构建和测试均通过，但 `ldd libcym_planner.so` 同时出现 ROS `cv_bridge` 使用的系统 OpenCV 3.2 与 `/usr/local` OpenCV 3.3。
- **原因**：CMake 在已经依赖 `cv_bridge` 的同时再次执行 `find_package(OpenCV)`；车端搜索优先命中 `/usr/local` 3.3，而 Melodic 的 `cv_bridge` 固定导出系统 3.2，导致同一进程加载两套 ABI。
- **修复**：删除 CymPlanner 独立的 `find_package(OpenCV)`、include 和 link 项，只使用 `cv_bridge` 从 ROS 环境导出的 OpenCV 头文件及库。
- **预防**：车端涉及 OpenCV 的插件除检查“无 not found”外，还必须检查版本唯一性；在 ROS Melodic 中优先复用 `cv_bridge` 导出的 OpenCV，不得无条件搜索 `/usr/local`。

### 2026-07-28：跨机 roslaunch 的 XML-RPC multicall 响应卡在 WSL 发送队列

- **现象**：小车两次执行无自动目标 `2026.launch`，都在 `load_parameters starting ...` 停住；Master 中只有 `/rosout`，没有底盘、激光或 move_base 节点。中断堆栈显示 Python 2 `xmlrpclib` 正在等待 `system.multicall` 的 HTTP 状态行。
- **原因**：最小化后，车端空 `system.multicall([])` 在 HTTP/1.1 下 10 次有 7 次超时，而同一请求在 WSL 本机 10/10 成功、HTTP/1.0 下 20/20 成功。ROS 会在服务端 Linux 内核早于 4.16 时自动禁用 HTTP/1.1；但 Master 位于新内核 WSL，无法感知远端小车仍为 4.9 内核，因此启用了会在当前 WSL 镜像网络路径中间歇卡住的 HTTP/1.1。WSL 端对应 TCP 连接的 275 字节响应会保持未确认，小车一直收不到 HTTP 状态行。
- **修复**：新增只对 WSL ROS Master 进程树生效的 `sitecustomize.py`，在明确的环境变量保护下把 ROS XML-RPC 请求处理器固定为 HTTP/1.0；`start_ros_master.sh` 启动前验证协议确为 HTTP/1.0，兼容文件缺失时拒绝启动。没有修改 `/opt/ros`、Windows 网卡卸载、防火墙或小车系统配置。
- **预防**：Master 启动行必须包含 `XML-RPC=HTTP/1.0`；静态启动前用车端空 multicall 连跑 20 次作为零运动回归。完整启动验收仍先观察 Master 是否出现 `/move_base`；若日志停在 `load_parameters`，同时检查两端 `ss -tpn` 和 roslaunch 堆栈，不得发送目标。

### 2026-07-28：main yolo2025 对照部署的校验范围与 CRLF

- **现象**：部署 `origin/main:yolo2025` 前，统一用 Python 2 编译全部脚本时，历史 `food.py` 的 Python 3 f-string 报语法错误；统一解析 launch 时又遇到空的 `nav_test.launch`；暂存包和正式包同时加入 `ROS_PACKAGE_PATH` 时，`roslaunch` 因两个 `yolo2025/2026.launch` 拒绝解析。正式替换后，Windows 生成的 tar 中脚本为 CRLF，`2026.py` shebang 含 `python2\r`。完成 LF 修复并启动静态对照后，`main` 仍在 45 秒内出现 IMU 与 AHRS CRC 错误；停止时旧版扫描回调还报告 `publish() to a closed topic`。
- **原因**：`main` 的历史包混合 Python 2、Python 3 和未指定版本的脚本，并保留空 launch；对照验证错误地假定整个包都属于同一解释器且所有 launch 都可独立解析。Windows 归档路径还对文本应用了 CRLF。关闭回调异常是 `main` 的旧版 `2026.py` 未包含当前分支已有的 shutdown 防护。CRC 在两套 yolo2025 中都出现，说明它不由当前 yolo2025 修改引起。
- **修复**：只按实际入口校验 Python 2 的 `2026.py`、Python 3 venv 的 `qrcode_scanner.py` 和非空 launch；替换后再执行唯一包的 `roslaunch --nodes`。车端对文本统一移除 CRLF，并用三个关键文件的 Git blob 哈希确认与 `origin/main` 完全一致。对照启动显式设置 `startup_goal_enabled:=false`，出现 CRC 后立即发布零速度并停止。
- **预防**：跨分支部署历史 ROS 包时按 shebang和实际 launch 依赖选择解释器，不得对整个 scripts 目录强行使用 Python 2；静态解析应跳过零字节历史文件，且同名 ROS 包不得同时出现在搜索路径。Windows 归档部署后必须检查 shebang 字节和 Git blob 哈希；对照旧分支时仍必须执行当前安全门，不能因“原版代码”而忽略 CRC、NaN、TF 或 USB 故障。

### 2026-07-28：把 mini 集成 IMU 误当成独立串口节点的风险

- **现象**：分析 `WXyuany/iFLYtek` 时同时看到 `ucar_controller` 和 `fdilink_ahrs`，容易直接按后者的 launch 使用 `/dev/ttyTHS1` 测试 IMU；实际小车的 `/dev/ttyTHS1` 用于雷达，mini 的 IMU 与底盘数据复用 CP2102。测试前还发现 CP2102 已从先前的根 Hub 直连恢复为外置 Hub `1-2.3`，物理条件已经变化。
- **原因**：上游 README 明确说明 mini 版在 `ucar_controller` 中集成 IMU，独立 `fdilink_ahrs` 面向晓版本；只看包名和默认 launch 会选错设备。上游两套解析器与本仓库一样，都没有系统检查每次串口 `read()` 的返回长度，一次短读可能被表现为 CRC 或帧长错误。
- **修复**：先以 `base_driver` 做 mini 正式路径静态测试，再停止它并让与上游完全一致的 `fdilink_ahrs` 临时独占 `/dev/ttyUSB0` 做差分。集成驱动约 4 分钟、独立驱动约 2 分钟均保持约 50 Hz 的有限 IMU 数据且无 CRC；测试前后零速，最后停止两个节点和 WSL Master。
- **预防**：任何 IMU 测试先用 `lsusb -t`、设备规则和上游车型说明确认数据路径；同一串口绝不允许两个节点并行打开。一次短测试通过只能说明当前未复现，至少连续观察 10 分钟并重复冷启动后才能认为硬件稳定。

### 2026-07-28：PowerShell 提前展开 WSL 命令中的 `$()`

- **现象**：在 PowerShell 中调用 `wsl ... bash -lc` 时，把 `$(rostopic type /imu)` 写进双引号命令，PowerShell 在进入 WSL 前先尝试执行本机 `rostopic`，产生“不是 cmdlet”错误并使后续采样顺序错位；随后一条内嵌 Python 统计命令也因多层引号形成 Bash 语法错误。
- **原因**：混用了 PowerShell、Bash 和 Python 三层引号及命令替换，没有先把采样拆成无需替换的简单命令。
- **修复**：去掉 `$()` 和复杂内嵌统计，分别执行 `echo`、`rostopic type`、`rostopic hz` 与 `rostopic echo`；重新采样确认 `/imu` 为 `sensor_msgs/Imu`、约 50 Hz，输出有限。
- **预防**：从 PowerShell 调用 WSL 时优先使用无命令替换的短命令；确需 `$()` 时必须保证它由 Bash 而非 PowerShell 解释。诊断统计先采集最小输出，不在一条命令中嵌套三种 shell 语法。

### 2026-07-28：关闭 RViz 后自动目标成功，但停车后仍有 AHRS CRC

- **现象**：关闭 RViz、使用 GitHub `main` 的 `yolo2025` 时，静态安全门正常，自动启动目标成功到达，二维码朝向扫描也完成；运动阶段采到约 `0.20～0.32 m/s` 和最高 `1.0 rad/s`。二维码流程结束约 44 秒、`/odom_raw` 速度已全零后，仍出现一次 `check crc16 faild(ahrs)`。内核没有新的 USB disconnect/reset。
- **原因**：RViz 关闭后复现明显变慢，说明跨机激光/地图/TF 流量及其引起的 CPU 调度可能放大驱动短读或串口失步，但 CRC 在没有 RViz、车辆已经停车时仍能发生，因此网络和电机瞬时电流都不是唯一条件。当前上游同款解析器未检查每段 `serial_.read()` 返回长度，仍无法仅凭 CRC 区分应用层短读与 UART 数据损坏。
- **修复**：运动前先以 `startup_goal_enabled:=false` 验证有限里程计和两段 TF；通过后零速重启为自动目标。CRC 出现后再次零速并停止完整 launch，同时停止 WSL Master；确认两端无 ROS 进程和 11311 监听残留。
- **预防**：评估 RViz 影响必须使用同一物理拓扑和同一完整 launch 做关闭/开启差分，不能把一次较晚复现当成修复。下一步优先给底盘驱动增加精确短读计数、期望/实际长度和重同步统计；在这些证据具备前，不得通过增大 TF 容差或继续长距离运动掩盖 CRC。

### 2026-07-28：main 旧版导航节点存在启动回调竞态

- **现象**：无目标静态启动时，`scan_cb` 在初始化完成前收到激光消息，访问尚未创建的 `production_global_obstacles_frozen`，抛出一次 `AttributeError`；随后节点完成初始化并继续运行。自动目标启动还曾短暂报告 `NO PATH`，之后稳定规划样本满足 5/5 并正常到达。
- **原因**：GitHub `main` 的旧版 `2026.py` 在创建订阅者后才初始化部分生产路线状态，回调可能抢先执行；启动早期地图和 TF 尚未齐备也会使第一次规划失败。
- **修复**：本轮只做对照，没有修改车端 `main`；以无目标预检确认节点随后恢复，自动目标由脚本已有的定位与规划稳定门控延后发送。
- **预防**：恢复当前分支时保留已有的初始化顺序和 shutdown 防护；对旧版做对照时必须区分一次性启动竞态与持续故障，若回调错误持续出现则不得发送目标。

### 2026-07-28：Python 2 实车监控脚本误用 math.isfinite

- **现象**：正式 `ucar_2026` 无 RViz 目标测试中，临时 Python 2 监控器的 `/odom_raw` 回调持续报 `AttributeError: 'module' object has no attribute 'isfinite'`，产生大量 rosout 错误并使该监控器无法给出里程计有限值和位移统计；move_base 状态监控仍工作，车辆最终到达目标并由独立命令确认速度归零。
- **原因**：`math.isfinite()` 是 Python 3 接口，小车 ROS Melodic 的监控器固定运行 Python 2.7；测试前没有先在目标解释器中执行最小兼容性检查。
- **修复**：立即独立发布零速度，并用 `rostopic echo`、`tf_echo` 和 `/move_base/status` 重新核验有限里程计、最终位姿和 `Goal reached.`；临时监控器未写入仓库。以后 Python 2 有限值判断使用 `not math.isnan(value) and not math.isinf(value)`。
- **预防**：任何临时安全监控脚本必须先用车端实际解释器做语法和 API 冒烟检查，再允许其参与运动测试；安全结论不得依赖已在回调中报错的监控器。

### 2026-07-28：正式 ucar_2026 首次无 RViz 目标出现短暂 NO PATH

- **现象**：静态 `/move_base/make_plan` 已生成路径，正式发布目标后约 5 秒连续两次出现 `NO PATH!` 和 `Failed to get a plan from potential`，同时一次全局 costmap 更新耗时 `0.678 s`；move_base 随后自行重新规划，约 31 秒后报告 `Goal reached.`。
- **原因**：该次错误与全局代价地图更新超时处于同一时间窗口，属于运行期代价地图更新和规划器重试的暂态失败；目标没有进入 aborted，不能据此判定整次导航失败。
- **修复**：保持安全监控，没有追加目标；到达后再次发布零速度，并核验最终 `map -> base_link` 约为 `(-1.72, 2.30, yaw 93.7°)`、`/odom_raw` 速度全零。停车后继续观察 45 秒，未出现 CRC、`head_len` 或 TF_NAN。
- **预防**：发目标前保留静态 make_plan 验证；运行中同时判断错误日志和 action 状态。单次 `NO PATH` 但 action 仍 active 时允许有限时重试，连续失败、位置不再更新或 action aborted 时立即取消目标并停车。

### 2026-07-28：PowerShell here-string 的 CR 尾字节污染远程 Bash

- **现象**：通过 PowerShell here-string 将多行脚本送入 `ssh ... bash -s` 时，最后一个 SHA-256 路径被解析为带 `\r` 的文件名，误报 JSON 不存在；后续静态检查末行的 `true` 也变成 `true\r` 并报告 command not found。前面的构建、测试和静态 ROS 检查均已执行，但末行结果不能直接采信。
- **原因**：Windows here-string 使用 CRLF，远程 Bash 在脚本最后一行保留了 `\r`；只检查本地文本可读性，没有先规范化传输内容。
- **修复**：将受影响的 JSON 哈希改为独立、单行 SSH 命令重新验证，确认与本地一致；RViz/进程检查也用独立命令复核。错误没有改变车端文件或触发任务运动。
- **预防**：跨 PowerShell/SSH 发送多行 Bash 前先移除 `\r`，或优先使用独立短命令；任何末行出现 `$'\r'` 或文件名带 CR 时，必须单独重跑该项，不能把前序成功混同为整条命令成功。

### 2026-07-28：车体投影误用局部 253 膨胀格作为实际接触

- **现象**：正式任务成功到达 52 并识别三个不同二维码后，切换 `body_projection` 前往 12；车辆在墙前持续报告 `blocked forward vehicle footprint`，恢复行为最终以 move_base status 4 中止。第二次最小复现命中固定格 `(-1.425, 1.545)`，代价为 253。
- **原因**：局部规划器只能直接拿到 local costmap 指针，旧实现便把完整 footprint 投影到局部代价地图，并沿用点模式的 `>=253` 阈值。253 表示内切膨胀危险区，不是 254 致命障碍；完整 footprint 再检查 253 等于把安全膨胀重复计算为实际接触。
- **修复**：`body_projection` 改订阅 `/move_base/global_costmap/costmap`；公开消息 99（原始 253）不再判接触，100（原始 254）才判致命接触，未知区单独安全拒绝。点模式继续使用原局部前视与 253 阈值，控制律和全部速度参数不变。
- **预防**：设计车体碰撞语义时必须同时写清“地图来源、原始代价值、消息转换值和未知区策略”；不能只写“检查 footprint”。实车阻塞日志必须至少记录命中的地图、格点与代价值，并用 253/254 回归测试锁定边界。

### 2026-07-28：PowerShell 展开 Bash `$pid` 留下零速度发布者

- **现象**：从 PowerShell 调用 WSL Bash 发布 1 秒零速度时，命令中的 `$pid` 在进入 Bash 前被 PowerShell 展开为空；`kill` 收到空参数，后台 `rostopic pub -r 10 /cmd_vel` 没有退出，并在 ROS Master 中留下发布者注册。
- **原因**：外层 PowerShell 使用双引号，误以为其中的 Bash 单引号会阻止 PowerShell 变量展开；实际 PowerShell 仍先处理 `$pid`。
- **修复**：按只读 `pgrep -af` 得到准确 PID 后结束该发布器和等待 shell，并执行 `rosnode cleanup` 清除失效注册；确认 `/cmd_vel` 无残留发布者后才启动正式任务。后续零速使用有界 `timeout 2 rostopic pub -r 10 ...`，不再跨 shell 传递 PID。
- **预防**：PowerShell 调用 `bash -lc` 时不得在外层双引号中使用 Bash `$变量`；优先使用无需变量的有界命令。任何诊断发布器结束后都检查系统进程和 `rostopic info /cmd_vel` 两层状态。

### 2026-07-28：catkin_test_results 使用了错误的结果子目录

- **现象**：三个 CymPlanner gtest 可执行文件共 10 项测试全部通过，但末尾执行 `catkin_test_results build/cym_planner/test_results` 报目录不存在，导致整条远程命令退出码为 1。
- **原因**：把包构建目录误当成 Catkin 测试结果目录；实际结果位于工作区 `build/test_results/cym_planner`。
- **修复**：使用 `catkin_test_results build/test_results/cym_planner` 重跑，汇总为 28 项、0 错误、0 失败。
- **预防**：测试命令统一写入 `docs/operations.md`，不要凭包构建路径推测结果路径；单项 gtest 输出成功但汇总命令失败时必须修正汇总路径并重新确认整体退出码。

### 2026-07-28：小车启动检查早于 WSL Master 就绪

- **现象**：冷启动 WSL Master 后约 2 秒立即执行 `start_2026.sh ... manual`，脚本的单次 3 秒 `rosnode list` 先超时并报无法连接；几秒后 Master 才完成日志目录检查并开始监听 11311。车端没有启动任何 ROS 节点或运动。
- **原因**：Master 首次启动的日志目录检查时间不固定，一键脚本只做一次连通检查，没有覆盖“进程已启动但 XML-RPC 尚未监听”的正常冷启动窗口。
- **修复**：一键脚本改为最多 10 次、每次 2 秒的有界检查，失败间隔 1 秒，并在首次失败时显示自动等待提示；约 30 秒仍不可达才退出。
- **预防**：跨机一键启动不能假定 roscore 进程创建即代表 Master 可用；连接门禁必须有限重试且最终失败即停止，快速文档仍要求看到 Master 的 `started core service [/rosout]`。

### 2026-07-28：诊断命令再次混入跨 Shell 变量与 sed 地址

- **现象**：从 PowerShell 调用 WSL 时，一条 make_plan 统计命令中的 Bash `$(...)`/`$poses` 没有得到预期文件和计数；最终里程计检查中的 `sed -n "/twist:/,$p"` 又把 `$p` 置于跨 shell 引号环境，生成无效 sed 地址。
- **原因**：为了压缩诊断步骤，把 PowerShell、Bash 变量、命令替换和 sed 地址表达式放在同一命令字符串里，重复了已经记录过的跨 shell 展开风险。
- **修复**：make_plan 直接管道到 `grep -c "pose:"`，不再创建 Bash 变量；里程计直接输出完整单帧，确认 twist 六轴均为有限零值。
- **预防**：PowerShell→WSL 的安全诊断命令必须保持无 `$变量`、无 `$()`、无依赖 `$` 的 sed/awk 地址；需要状态时拆成多条直接命令，不能为了少一次调用重引入多层解释器。

### 2026-07-28：停止脚本在加载 Melodic 前启用 nounset

- **现象**：任务已因点 16 转后误差超限而停车，执行 `stop_2026_task.sh` 时立即在 `/opt/ros/melodic/etc/catkin/profile.d/1.ros_distro.sh` 报 `ROS_DISTRO: unbound variable`，未能停止 roslaunch。
- **原因**：停止脚本与早期启动脚本犯了同一错误：在 `source /opt/ros/melodic/setup.bash` 前使用 `set -u`；Melodic 环境脚本会读取尚未定义的 `ROS_DISTRO`。
- **修复**：先启用 `set -e -o pipefail`，保存调用者网络变量并完成 Melodic/工作区两次 source，之后再启用 `set -u`。失败后已按精确核对的 roslaunch PID 发送 SIGINT并确认链路正常退出。
- **预防**：仓库中所有加载第三方 ROS 环境的 shell 入口都必须在 source 后启用 nounset；启动脚本与停止脚本要共用相同的空白登录 shell 回归检查，不能只修一侧。

### 2026-07-28：生产点旋转后只有误差终止、没有回中恢复

- **现象**：任务成功到达点 16，首次到达误差为 `0.099 m`；完成 360° 旋转后误差变为 `0.102 m`，仅比 `0.100 m` 安全上限多 `2 mm`，任务直接以 `post-turn point 16 drifted` 终止。同期 move_base 状态为 `Goal reached.`，从当前位置到 28 和 170 均能生成全局路径，因此不是找不到路径。
- **原因**：状态机在旋转后直接调用最终位置校验，没有为轮胎打滑、地面不平或定位微小漂移提供受控恢复步骤。
- **修复**：增加转后回中阶段；误差超过 `0.06 m` 时，保持原定的下一点朝向，通过已有 `move_base` 向当前点中心最多重发 2 次目标。每次回中仍经过有限里程计、TF、全局规划和 action 状态安全门；回中导航失败或最终超过 `0.10 m` 时只告警并继续后续任务，避免微小漂移中断整场任务。NaN、TF 失效和传感器掉线仍停车终止。
- **预防**：原地旋转后的“恢复触发阈值”和“终端告警阈值”必须分开，前者应留出余量且小于后者；不得简单放宽误差掩盖漂移，也不得在已有安全导航链路之外另写直接平移控制。任务完成优先的非致命恢复不得吞掉硬件和定位安全故障。

### 2026-07-28：误把 roslaunch dump-params 输出文件当成位置参数

- **现象**：车端静态验证时执行 `roslaunch --dump-params /tmp/... ucar_2026 2026.launch`，roslaunch 把临时文件名解析为包名并以 `RLException` 退出；没有启动任何 ROS 节点。
- **原因**：未先查看 Melodic `roslaunch --help`，误以为 `--dump-params` 后面接受输出路径。
- **修复**：确认该选项只把参数写到标准输出，改为 `roslaunch --dump-params ucar_2026 2026.launch ... > /tmp/...` 后重新验证。
- **预防**：不熟悉的 ROS CLI 选项先查目标系统版本的 `--help`；需要保存标准输出时使用 shell 重定向，不自行猜测选项参数。

### 2026-07-28：使用一次性进程号读取已退出任务环境

- **现象**：部署前检查发现任务 PID `28074`，完成文件同步后再读取 `/proc/28074/environ` 时进程已经退出，命令报文件不存在。
- **原因**：把瞬时只读检查得到的 PID 当成跨多条 SSH 命令稳定存在，没有在使用前重新确认。
- **修复**：重新按进程特征检查，确认车端任务已经退出；后续验证不依赖旧 PID。
- **预防**：读取 `/proc/<pid>` 前必须在同一远程 shell 中重新解析并验证 PID，或直接按当前 ROS 网络配置查询；不得跨命令缓存短生命周期进程号。

### 2026-07-28：误用 rosrun --help 启动了任务节点

- **现象**：为了确认 Catkin 入口，执行 `rosrun ucar_2026 production_task_2026.py --help`；ROS Python 节点没有普通 CLI 帮助语义，进程实际启动并等待 move_base，而不是打印帮助后退出。
- **原因**：把 ROS 节点入口当作常规命令行程序验证，没有使用只读的 `roslaunch --nodes`、`rospack find` 或文件链接检查。
- **修复**：按精确 PID 先发 SIGINT、再发 TERM，确认命令退出；车端当时没有 move_base 或导航 launch，节点只在等待 action server，未发送目标。随后额外发布有界零速度并检查无残留。
- **预防**：禁止用 `rosrun <node> --help` 验证 ROS 节点可执行性；静态检查使用 `python2 -m py_compile`、`roslaunch --nodes`、`rospack find` 和文件哈希，任何会调用 `rospy.init_node` 的入口均按真实启动处理。

### 2026-07-28：OCR 接口假设、远程超时与 launch 上传目录连续出错

- **现象**：未先确认车端既有 OCR 接口就按 Tesseract 设计并启动 `apt-get update`；本地 SSH 工具超时后，远端 apt 子进程仍继续运行。随后车端构建命令再次把远端 `awk` 放进 PowerShell 字符串，命令在本地解析失败。部署时又把 `launch/2026.launch` 与包根文件放在同一个多源 `scp` 中，导致它以 basename 误传为 `~/ucar_ws/src/ucar_2026/2026.launch`，与正式 `launch/2026.launch` 重名，`roslaunch` 静态解析拒绝继续。
- **原因**：没有在实现前只读检查用户已有的 `/home/ucar/ocr3/ppocr_trt/python/live_ppocr.py`；把 SSH 客户端超时误当成远端进程一定结束；重复使用已经在档案中禁止的跨 PowerShell/SSH/Bash 变量解析；多源 `scp` 只能使用共同目标目录，却没有保留 launch 的子目录语义。
- **修复**：用户指出接口后立即取消本地等待，并精确终止仍在运行的 apt PID，`dpkg --audit` 无异常且未安装 Tesseract。OCR 改为持久 Python 3 helper 直接加载 `live_ppocr.py` 的 CUDA 引擎。车端网络值改用本次动态发现后已核验的显式 `192.168.8.199/192.168.8.231`，不再嵌套 `awk`。新版 launch 单独上传到 `~/ucar_ws/src/ucar_2026/launch/2026.launch`；核验误传文件绝对路径后删除包根副本，并确认本地副本可恢复、两端哈希一致、`roslaunch --nodes` 通过。
- **预防**：接入 OCR/相机等已有能力前先检查用户指定脚本、README、模型和实际输出协议；任何 SSH 超时后先查远端 PID/锁再重试；跨 Shell 网络发现与实际命令必须拆开；不同远端子目录的文件禁止合并到一个 `scp` 目标。部署后固定执行“远端相对路径哈希 + `find` 同名文件 + `roslaunch --nodes`”三项检查。

### 2026-07-29：ROS 相机退出后的 V4L2 暂态失败没有重试

- **现象**：完整任务安全到达 52 并识别三个不同二维码，释放 `/usb_cam` 后启动 Python 3 OCR helper；OpenCV 连续两次报告像素格式不支持和设备忙，helper 立即退出，任务以 `ABORTED` 停车，未进入生产路线。
- **原因**：原 `open_camera()` 只做 V4L2 和默认 backend 两次紧邻尝试，没有覆盖 ROS 节点退出、V4L2 stream-off 和设备重新协商之间的短暂交接窗口；OCR 模型与 `/dev/ucar_video` 软链接、设备支持的 YUYV/MJPG 格式均正常。
- **修复**：增加由 `camera_open_timeout` 约束的多次打开/预热重试，并明确请求 YUYV；新增 Python 3 fake-camera 回归测试，先确认旧实现因第三次才能成功而失败，再确认修复通过。车端构建、原有 15 项测试、新增 1 项测试和真实 `usb_cam` 释放后 0.1 秒切换均通过。
- **预防**：独占硬件从一个进程交接到另一个进程时不能把 `rosnode kill` 返回等同于设备已稳定；打开、格式协商和首帧都要有短周期有界重试，最终超时仍必须零速终止。

### 2026-07-29：车辆离开起点后重启固定初值定位链路

- **现象**：首次任务在 52 因相机交接失败并停止完整 launch；未先把车放回起点就重启 mission。`lidar_loc` 再次采用 `(-0.25, 2.75, 0)` 固定初值，而实车位于 52，`map -> base_link` 与实际位置不一致，任务开始向 52 发送错误定位下的导航运动。
- **原因**：把“里程计、TF 都是有限值”误当成“全局地图位姿正确”，忽略了完整定位进程重启会丢失上一轮地图位置，且当前 launch 的初值只适用于实车物理位于起点。
- **修复**：发现地图位姿与已知实车位置不符后立即发布零速度并停止完整链路；等待人工把车放回起点后才允许重启。操作文档新增中途终止后的重启门禁。
- **预防**：安全门除有限值和新鲜度外，还必须核对启动时实车已知位置与 `map -> base_link` 初值一致；固定初值定位方案下，任何离开起点后的整链重启都要求先物理复位或提供经过验证的当前初值。

### 2026-07-29：实车监控命令再次被 PowerShell 多层引号破坏

- **现象**：包含正则字符、Bash 循环变量和 JSON 文本的三条 PowerShell→SSH 命令分别出现 PowerShell ParserError、远端循环变量被提前展开和命令未实际执行；一次直接 SSH 启动还因本地超时返回 124，但远端 roslaunch 已经启动。
- **原因**：为了合并诊断步骤，再次把 PowerShell、SSH、Bash、正则和 JSON 放进同一个双引号字符串，违反了档案中已有的“无跨 shell 变量、拆分短命令”规则；同时把客户端超时误当成远端启动失败。
- **修复**：后续检查改用无 `$变量` 的短远端命令；长时 mission 使用明确的远端后台启动并通过 `pgrep`、日志和 ROS 状态二次确认；失败的探针全部重新执行并核验真实进程状态。
- **预防**：PowerShell→SSH 实车命令不得内嵌 Bash 循环变量、复杂字符类或原始 JSON；需要结构化输入时先编码或使用已部署脚本。任何 SSH 超时后第一步只能查远端 PID/ROS 节点，禁止盲目重启。

### 2026-07-29：完整 supervisor 拉回 ROS 相机导致原生相机持续设备忙

- **现象**：任务结束二维码阶段后停止 `/usb_cam`，但完整 supervisor 随即重新拉起该节点；Python 3 helper 在约 8 秒内连续 66 次打开 `/dev/ucar_video` 均遇到设备忙，无法进入生产 OCR。
- **原因**：只在任务 launch 中关闭了相机 respawn，没有覆盖外层完整 supervisor 的进程恢复策略；“杀掉 ROS 节点即可稳定释放设备”的假设不成立。
- **修复**：生产 OCR 改为继续使用 `/usb_cam/image_raw`；Python 2 ROS 节点通过 `cv_bridge` 保存当前帧，Python 3 helper 只读取保存的图片并调用既有 CUDA OCR，不再争抢 V4L2 设备。
- **预防**：独占设备方案必须检查整条进程监管链，而不只检查当前 launch；若现有 ROS 图像链满足质量和时延要求，优先复用同一采集进程。

### 2026-07-29：Python 2 rospy Duration 方法名写成 toSec

- **现象**：ROS 图像等待逻辑在 Python 2/Melodic 运行时报 `Duration` 没有 `toSec` 属性。
- **原因**：混用了其他 ROS 客户端风格的方法名；rospy 的正确接口是 `to_sec()`。
- **修复**：改用 `to_sec()`，车端重新构建并通过相关测试。
- **预防**：ROS Python 时间接口以目标系统 Melodic 的 rospy 实际 API 为准；新增或修改兼容代码后必须在车端 Python 2 执行。

### 2026-07-29：点 3 热恢复反向返回 52 导致碰撞

- **现象**：车辆不在标准起点流程状态时热重启生产任务，状态机从点 3 反向返回 52，用户报告发生碰撞。
- **原因**：中途恢复复用了包含二维码前置阶段的完整状态机，当前地图位姿、物理位置和预期阶段不一致，产生了非标准的 `3 → 52` 路径。
- **修复**：立即取消目标、发布零速度并停止完整链路；等待车辆物理放回起点后，按标准完整流程重启。后续干净运行未再走该反向路径。
- **预防**：固定初值定位下禁止把热恢复当作任意位置续跑；恢复入口必须显式限定阶段和当前位置，否则只能物理复位到起点后完整重启。

### 2026-07-29：7→8 全局直线可规划但局部车体轮廓连续阻挡

- **现象**：标准实车流程成功到达点 7，前往点 8 时车辆看起来继续向外侧移动，随后在距点 8 约 `0.21 m` 处停止；日志重复报告 `blocked forward vehicle footprint`、`NO PATH!`，旋转恢复也因潜在碰撞拒绝，最终 status 4。
- **原因**：点 7 `(0.75, 1.25)` 到点 8 `(1.25, 1.25)` 本来就是地图 +X 的向外路线；全局 make_plan 复核为近似水平直线，y 约 `1.245–1.250`，并未绕行。实车停止位姿约 `(1.07, 1.15)`，相对规划线已有约 `0.10 m` 横向偏差，局部车体轮廓检查随后判定前方不可安全通过。
- **修复**：没有强行绕过碰撞检查；任务安全结束并停止全部 ROS/小车进程。是否保留 `7 → 8` 必须由用户确认，定位/控制横向偏差也需在下一次运动前单独验证。
- **预防**：区分“编号路线方向”和“局部跟踪偏差”；争议路段先同时保存点坐标、全局 plan、实车 TF 和 footprint 命中日志。局部安全检查连续阻挡时不得通过放宽阈值或强制速度继续。

### 2026-07-29：实车诊断命令的调用格式三次不准确

- **现象**：首次用 `python2 -m unittest <文件路径>` 导致模块导入错误；首次 `/move_base/make_plan` YAML 缺少外层 `map`/分隔符；停机时有界 `rostopic` 在 Master 退出过程中收到 `init_node` 中断。
- **原因**：把文件路径误作 unittest 模块名、手写服务 YAML 结构不完整，并在停机竞态中仍尝试初始化新的 ROS 诊断节点。
- **修复**：测试改为直接运行测试文件并得到 10 项通过、2 项按环境跳过；make_plan 使用完整请求后得到 7→8 直线路径；停机改由进程和 11311 端口双重检查确认无残留。
- **预防**：车端测试、服务请求和停机核验分别使用已验证的固定命令模板；停机阶段优先检查现有进程与监听端口，不额外依赖即将消失的 Master 新建 ROS 节点。

### 2026-07-29：连续 OCR 修改前的组合只读检查被可选路径拖成整体失败

- **现象**：把 `MODEL_ROUTING.md`、代码搜索、领域文档和 Git 状态合并为一次编排，其中一个可选路径返回退出码 1，导致整组调用显示失败，只留下部分输出。
- **原因**：把“可不存在”的领域文档检查与必须成功的代码读取放进同一组，并让任一非零退出码影响整组结果。
- **修复**：拆成独立短命令，分别确认 `MODEL_ROUTING.md`、`CONTEXT.md`/ADR、相关代码和工作树状态；没有修改文件或启动 ROS。
- **预防**：必需输入和可选输入不得共用整体成功条件；可选文件先用 `Test-Path` 判定，代码搜索无命中也要与读取失败区分。

### 2026-07-29：感知测试补丁使用了错误的导入上下文

- **现象**：第一次给 `production_task_perception.py` 和测试文件同时打补丁时，测试文件导入列表与假设的顺序不同，`apply_patch` 校验失败。
- **原因**：依据截断的搜索输出构造了过于具体的上下文，没有先读取测试文件开头的真实内容。
- **修复**：确认补丁未写入任何文件后，读取精确行内容并拆成小补丁，随后语法检查和 7 项感知测试通过。
- **预防**：截断输出只用于定位，不能直接作为补丁上下文；跨文件补丁前分别读取目标区域，失败后先确认原子性再重试。

### 2026-07-29：首次连续 OCR 方案阻塞 action 监控且对准绕过碰撞保护

- **现象**：独立 Sol 安全复核发现，移动阶段直接同步调用 OCR 最长可阻塞主循环 12 秒；候选返回时可能已经到达或导航失败。旧视觉对准又由任务节点直接发布非零 `/cmd_vel`，不经过 move_base/CymPlanner 的车体轮廓检查，且雷达采样前没有再次确认残余角速度为零。
- **原因**：最小实现复用了原先“车辆已经停在固定点”的同步 `capture_ocr()` 和直接角速度对准函数，没有重新审视它们在移动状态机中的并发与碰撞语义。
- **修复**：移动 OCR 改为单任务异步 worker，主循环持续检查 action、安全门和目标超时；候选消费前重新检查目标活动状态，并记录取帧请求位姿。对准改为 move_base 同位置小角度朝向目标，每次对准后和雷达采样前都要求连续新鲜低速里程计样本。
- **预防**：把停车阶段能力迁移到移动流程时，必须逐项审查“是否阻塞主监控、结果是否可能过期、任何非零速度由谁做碰撞检查、传感器采样前是否重新静止”；实车部署前必须进行独立高能力安全复核。

### 2026-07-29：读取连续 OCR 函数片段的 PowerShell 命令意外超时

- **现象**：一个只读 `Get-Content`/`Select-String` 命令已输出目标函数前半段，却在约 11 秒达到工具超时并返回 124。
- **原因**：为动态计算起始行又格式化较大范围内容，命令执行时间超过默认预期；该读取不应依赖复杂组合。
- **修复**：改用已知行号的短范围独立读取获得剩余内容；没有修改文件或启动 ROS。
- **预防**：代码片段定位先用 `rg -n` 得到行号，再用固定短范围读取；不要为一次查看组合全文搜索和逐行格式化。

### 2026-07-29：异步修复后仍遗漏复查与取消之间的终点竞态

- **现象**：第二轮 Sol 复核确认异步 OCR 和受保护对准已关闭原阻断项，但发现候选消费前的 action 状态复查与随后 `cancel_goal()` 之间仍有极短 TOCTOU 窗口；目标可能在窗口内从 ACTIVE 变为 SUCCEEDED，而调用方仍进入观察。
- **原因**：取消函数虽然返回最终 action 状态，调用方最初忽略了返回值，并把 SUCCEEDED 与 PREEMPTED/RECALLED 统一当作“可以观察”。
- **修复**：记录取消前状态并接收取消后的最终状态；只有 PREEMPTED/RECALLED 才进入观察。若返回 SUCCEEDED，则删除迟到移动帧、验证段终点并结束当前段。新增 ACTIVE→SUCCEEDED 竞态测试。
- **预防**：状态复查不能替代原子操作；任何“检查后执行”的 action 流程都必须在执行结果处再次分流，并为状态跨越窗口建立回归测试。

### 2026-07-29：直接运行 rospy 状态机单测时没有初始化时间

- **现象**：车端构建成功后直接用 Python 2 执行几何/状态机测试，11 个纯测试和其余 ROS 策略测试通过，但 3 个新取消竞态用例在 `rospy.Time.now()` 报 `ROSInitException: time is not initialized`。
- **原因**：测试通过 `object.__new__` 隔离任务对象以避免启动 ROS，却没有像 rostest/真实节点那样初始化 rospy 的 wall-clock 时间；本机因没有 ROS 模块而跳过了这些用例，未提前暴露。
- **修复**：在 ROS 策略测试夹具 `setUp()` 中显式调用 `rospy.rostime.set_rostime_initialized(True)`，不启动节点、不连接 Master，也不改变生产任务代码。
- **预防**：任何直接执行且会调用 `rospy.Time.now()` 的无节点单测，都必须在夹具中初始化 rostime或注入假时钟；本机跳过的 ROS 用例必须在车端、实车运动前执行。

### 2026-07-29：WSL Master 后台启动与组合端口探针没有给出有效结论

- **现象**：第一次用 WSL 内部 `nohup` 启动 Master 没有返回 PID且进程未保留；随后把 WSL 日志检查与 Windows `Test-NetConnection` 合并执行，TCP 探针超时使整组调用返回 124。
- **原因**：假定短命令退出后 WSL 会保留其后台子进程，同时把可能较慢的 Windows 网络探针与关键 WSL 进程检查绑定为一个成功条件。
- **修复**：按规则用隐藏的 `Start-Process wsl.exe` 持有前台 `start_ros_master.sh`，独立读取日志，确认实际 Master 地址为 `192.168.8.198`、HTTP/1.0 生效且 11311 正在监听；mission 前车端连通检查通过。
- **预防**：WSL 长驻服务由 Windows 持有前台进程或使用经过验证的服务机制；Master 地址只信任启动日志；端口、进程和日志检查保持独立。

### 2026-07-29：点 1 转入 1→31 时车体对角投影命中左墙

- **现象**：连续 OCR 实车任务以 `0.019 m` 误差到达点 1，`1→31` 全局路径有 299 个位姿，但局部规划器立即报告车体轮廓命中致命格 `(-2.485,1.215)`，随后 `blocked forward vehicle footprint`；恢复旋转也判潜在碰撞，任务 status 4 安全终止。
- **原因**：点 1 `(-2.25,1.25)` 距左墙 `x=-2.50` 和上墙 `y=1.50` 都只有约 `0.25 m`。真实 footprint 为 `0.342 m × 0.256 m`，90° 转向中间角度的最大轴向投影约 `0.214 m`；计入墙体致命格厚度、栅格离散和约 2 cm 到达误差后几乎没有余量。问题不是没有全局路径，而是角点转向的真实车体安全空间不足。
- **修复**：没有缩小 footprint、切回点碰撞或强制速度；任务和全部 ROS 链路已停止。本轮结果保留一个移动候选，但停车复拍 6 次为空，因此正式墙点仍为 0。
- **预防**：外圈角点不得把“中心点可达”等同于“能在该点安全旋转”。优先把导航目标向场地内部做小幅安全偏置并保持真实 footprint；任何偏置必须先做静态路径/轮廓验证并由用户确认后再实车。

### 2026-07-29：首次红灯命令没有显式构建新增 gtest

- **现象**：同步失败测试后执行 `catkin_make --pkg cym_planner` 显示库构建成功，没有暴露测试引用的缺失头文件。
- **原因**：普通 `catkin_make` 不保证构建 `CATKIN_ENABLE_TESTING` 下的每个 gtest 可执行目标，把“库构建成功”误当成“新增测试已编译”。
- **修复**：显式执行 `catkin_make --pkg cym_planner --make-args cym_planner_escape_recovery_test`，测试按预期因缺少 `escape_recovery.h` 红灯；实现后再执行整个 `run_tests_cym_planner`。
- **预防**：TDD 红灯必须直接构建或运行新增测试目标，不能只看包的默认构建结果。

### 2026-07-29：替换 footprint 函数签名时残留占位字符

- **现象**：一次分段补丁把旧函数签名暂时替换成单独的 `Q` 字符。
- **原因**：构造大范围替换前使用了无意义占位符，补丁原子成功但源码在中间状态不可编译。
- **修复**：在任何同步前用固定行号复读发现并用完整 `inspectFootprint` 签名和实现替换；该中间状态未部署到小车。
- **预防**：函数级重构直接一次性替换完整签名，禁止写无语义占位符；每轮补丁后先 `rg` 搜索异常标记并执行 `git diff --check`。

### 2026-07-29：按旧版 Costmap2DROS 记忆调用 getRobotPose

- **现象**：首次车端编译受限横移恢复时，`getRobotPose(tf::Stamped<tf::Pose>&)` 无匹配重载。
- **原因**：依据其他 ROS 版本的接口记忆编写适配层；车端 Melodic 安装实际接口是 `getRobotPose(geometry_msgs::PoseStamped&)`。
- **修复**：按 `/opt/ros/melodic/include/costmap_2d/costmap_2d_ros.h` 的编译诊断改为直接传入 `PoseStamped`，车端重新构建成功。
- **预防**：涉及 ROS 版本差异的 C++ API 必须以车端头文件和编译器为准，不能只凭跨版本记忆；运动代码部署后先构建测试，不启动节点。

### 2026-07-29：首版受限横移通过单测但仍有三个 P1

- **现象**：车端编译与纯函数测试全部通过，但 Sol 最终复核发现：活动横移会在前视阻挡临时消失时提前清空状态；footprint 扫描遇第一个阻挡格就返回，不能判断接触是否变多；横移预览只看全局 OccupancyGrid，没有把本地动态障碍纳入决策。
- **原因**：测试只覆盖方向、布尔判据和预算纯函数，没有覆盖 `computeVelocityCommands` 的跨周期状态、完整多格 polygon 或全局/本地代价地图组合，形成了关键路径假覆盖。
- **修复**：把预防置于优先，拆分两模式参数并降低 `mode2_body_projection` 默认速度；新增 `escape_enabled`，YAML 设为 `false`，源码硬制为关闭并拒绝任何 `true` 请求，使三个 P1 路径当前不可达。
- **预防**：任何会输出非零恢复速度的功能，必须建立跨周期本地规划器集成测试、全 polygon 接触计数测试和本地动态障碍测试；纯方向单测不能作为实车启用依据。

### 2026-07-29：拆分低速参数后仍遗漏正常控制的近车盲区和 NaN

- **现象**：第二轮 Sol 复核确认脱困已关闭、两模式参数已分离，但发现正常碰撞检查从约 20 cm 前的 `target_index_` 开始，跳过当前车体到目标索引之间；同时 NaN 进入原 `clampValue` 的 `std::min/std::max` 组合可能产生负最大角速度。
- **原因**：把“降低速度”误当成完整预防，没有按实际候选 Twist 检查矩形车体的旋转扫掠；限幅函数也没有实现非有限值失败关闭。
- **修复**：路径检查改为先检查当前真实车体，再从全局路径离当前车体最近的点开始；模式 2 对实际 Twist 每 0.025 s 投影未来 0.40 s，同时检查全局和本地代价地图；位姿、误差、周期和最终 Twist 任一非有限都保持零速并返回失败，限幅函数对非有限输入返回零。
- **预防**：运动参数调整后的复核必须覆盖“当前车体到前视点”的完整区间和实际 `(v,w)` 扫掠；所有浮点控制链在限幅之前都要有显式 finite 门禁。

### 2026-07-29：组合校验命令再次被引号和超时破坏

- **现象**：一次本地哈希/YAML 组合检查触发 PowerShell ParserError；随后一次远端 Python YAML 检查因多层引号使文件路径被拆开，报 `IOError`。最终收尾又把整份档案读取、多个搜索和 `git diff --check` 合在一个 10 秒命令中，虽已输出关键结果仍以超时 124 结束。
- **原因**：把 PowerShell、SSH、Python 字符串和含路径的表达式继续嵌套在一条命令中，又把多个成功条件不同的只读检查放进同一个短超时调用，没有遵循既有的短命令规则。
- **修复**：没有依据失败输出修改代码或远端文件；改为分别执行哈希、YAML、进程和 diff 检查，并通过标准输入把 YAML 内容传给远端 Python 解析，避免远端文件路径和 Python 字符串嵌套。
- **预防**：PowerShell 到 SSH 的校验命令保持单一职责；结构化文件优先经标准输入解析，禁止在三层 Shell 中拼接带引号的路径和复合表达式；整份档案读取不得与仓库级 diff 检查共用超时和退出码。

### 2026-07-29：性能优化勘察再次组合超时并误用跨 Shell 替换

- **现象**：一次把 launch、任务脚本、规划器源码和多个配置合并读取的命令在 20 秒超时；一次查询车端 `usb_cam` 时在 PowerShell 双引号内写了远端 `$(rospack find usb_cam)`，被本机提前解释；还按不存在的状态机测试文件名和 PowerShell 不支持的 `*.py` LiteralPath 查找，产生路径错误。一次并行红灯检查因其中一项预期失败而只返回一个结果。收尾时两次尝试删除本地 `__pycache__` 被安全策略拒绝；一次组合验证中远端 YAML Python 字符串被 SSH 引号破坏，测试与 launch 解析已经成功却使整条命令退出 1；第一次改用标准输入时又把 `YAML_OK` 的字符串引号丢失成变量名。最后为查一个 Costmap2D 运算符又对车端多个大目录递归 `grep`，在 24 秒超时。
- **原因**：没有继续坚持“一次只读一个边界”的短命令规则，且在已记录的 PowerShell→SSH 场景中再次使用 `$()` 和多层字符串；测试发现也没有先用 `rg --files`。对生成缓存采用了不必要的删除动作，并继续在远端 Python 内输出带引号字符串。
- **修复**：所有失败均为只读、被策略拦截或预期红灯，没有启动 ROS、修改远端运行状态或依据不完整输出做结论；生成缓存未被纳入改动。随后使用显式 `/home/ucar/ucar_ws/src/usb_cam` 路径、`rg --files` 和独立命令重新核验；C++ 与 Python 红灯分别得到预期的缺失 helper/方法错误；4 份 YAML 改为分别从标准输入解析，并只输出数字，全部通过；Costmap2D 声明改为只读取已知头文件。
- **预防**：大型源码勘察按文件和函数拆分；PowerShell 到 SSH 禁用 `$()`；未知测试路径先 `rg --files`；预期失败的 TDD 命令用独立调用或 `Promise.allSettled`，避免一个红灯遮住另一个结果；不为收尾验证清理无关缓存；结构化文件验证禁止在三层 Shell 中嵌套路径或字符串引号；已知系统头文件位置时禁止从 `/opt/ros` 和整个工作区做无界递归搜索。

### 2026-07-29：构建后静态 launch 检查漏加载工作区环境

- **现象**：相机停止兜底的车端构建与 30 项测试已经成功，随后同一 SSH 命令中的 `roslaunch --nodes` 报找不到 `ucar_2026/2026.launch`。
- **原因**：该远端 shell 只加载了 `/opt/ros/melodic/setup.bash`；`catkin_make` 完成不会自动把新生成的 `devel` 环境注入当前 shell，静态 launch 检查因此没有工作区包路径。
- **修复**：没有启动 ROS 或修改运行状态；在新短命令中依次加载 Melodic 和 `/home/ucar/ucar_ws/devel/setup.bash` 后重跑，节点解析成功，且两处相机挂起参数均为 `true`。
- **预防**：任何 `rospack`、`roslaunch` 或工作区动态库检查，即使紧跟 `catkin_make`，也必须在同一检查命令前显式重新 source 工作区 `devel/setup.bash`；不得假定构建步骤会改变父 shell 环境。

### 2026-08-03：首次 local footprint 实车任务在点 3 安全中止

- **现象**：车辆从固定起点通过有限里程计、两段 TF 和雷达安全门；先成功到达 52（误差 `0.031 m`）、完成三方向 QR（a/d/i）和相机启停。切至 `body_projection` 前往点 3 `(-1.250, 1.250)` 时，CymPlanner 连续报告 local raw 254 致命格 `(-1.455, 1.455~1.485)` 命中真实 footprint，并在候选 Twist 的 `0.225 s` 扫掠中被阻挡；全局重规划、清图和旋转恢复均无安全解，move_base status 4。任务立即零速并停止，车辆最后 map 位姿约 `(-1.314, 1.588)`，不再处于起点。
- **原因**：点 52 至点 3 的进入/转向几何使真实矩形车体接触中间区域的静态墙致命格。此前仅完成编译、单测和 launch 静态解析，未对这条实车进入路径做 local StaticLayer + 完整 footprint 的在线验证。
- **修复**：保持 raw 254/255 的失败关闭策略，不缩小 footprint、不放宽阈值、不强制穿过；发布零速，停止车端 launch 和 WSL Master，清除死亡 ROS 注册并保留日志与结果文件。
- **预防**：完整任务前，对每个模式切换后的首个目标先做静态 global plan 与 local footprint/转向扫掠验证；尤其检查中间墙角的 `52→3` 进入段。发生 status 4 后不得原地重启固定初值任务，必须先把车物理放回起点。

### 2026-08-03：实车启动诊断误传位置参数与无效 cleanup 选项

- **现象**：首次把 `check` 作为 `start_2026.sh` 的第一个参数，脚本将其解释为 Master IP 并安全拒绝；一次 WSL Master 检查遗漏 ROS 环境而找不到 `rosparam`；又把 ROS 2 风格的 `rosnode cleanup -y` 用于 Melodic，命令不支持该选项。停止 WSL Master 的组合检查因目标进程已退出返回非零。
- **原因**：没有先按脚本的 `MASTER_IP [mode]` 位置参数契约执行，并把不同 ROS CLI 版本与“目标已停止”的检查混入同一命令。
- **修复**：网络路由和 11311 连通性均独立确认后，使用正确的 `start_2026.sh 192.168.8.198 check`；失效节点改为交互确认的 `printf 'y\\n' | rosnode cleanup`；所有错误路径均未发送自动目标或改变安全策略。
- **预防**：实车脚本先读取 usage/首行参数定义；ROS CLI 选项以 Melodic `--help` 为准；停止后的 PID/进程检查允许“已不存在”作为成功状态，不应与关键停机操作共用退出码。

### 2026-08-03：本机 YAML 校验误用 Windows 默认 GBK

- **现象**：校验带中文 UTF-8 注释的 `ucar_cym_planner_params.yaml` 时，Python 3 以 Windows 默认 GBK 打开文件，抛出 `UnicodeDecodeError`；代码和配置均未被该检查改写。
- **原因**：校验命令省略了文本编码，错误地依赖终端默认编码。
- **修复**：后续 YAML 校验显式使用 `open(path, encoding='utf-8')`，再调用 `yaml.safe_load`。
- **预防**：Windows 本地对仓库 UTF-8 文本的 Python 读取必须显式编码；编码检查失败不能当作 YAML 语法失败，更不能据此修改配置。

### 2026-08-03：PowerShell 将 rg 路径通配符原样传递

- **现象**：本机 `rg` 检查把 `docs/changes/2026-08-03*` 当作字面文件名，返回 Windows 路径语法错误；没有修改文件、部署或启动 ROS。
- **原因**：PowerShell 不会像 Bash 一样为原生命令展开该路径通配符。
- **修复**：改为传目录或使用 `rg --glob '2026-08-03*'`。
- **预防**：Windows 下对原生命令的路径筛选使用 `rg --glob`，不要把 Bash glob 直接作为路径参数。

### 2026-08-03：弹性局部路径首轮复核发现姿态和周期状态漏洞

- **现象**：初版局部带只插值位置、使用中心线朝向验证候选；相同 global plan 的周期性
  `setPlan()` 又会清空 active 带和 0.4 秒搜索计时。前者可能漏掉转弯扫角碰墙，后者会
  在 3 Hz plan 更新下无限零速等待；均在部署前的独立安全复核中发现。
- **原因**：把“0.4 秒实际 Twist sweep”错误当作整条局部带的姿态证明，并假定 `setPlan()`
  只在路线实质变化时调用。
- **修复**：候选改用变形后切线、平移与 yaw 联合采样；弹性阶段增加 `0.07 m/s`、
  `0.30 rad/s` 上限。对 global plan 按弧长采样 7 点比较位置与切线，仅整段等价时保留
  active 带和搜索计时；显著改道才清除。
- **预防**：局部路径安全验证必须覆盖完整 `(x,y,yaw)` 连续扫掠和规划器跨周期状态；
  对任何周期性输入先验证其重入语义，不能依赖“通常只在变更时回调”的假设。

### 2026-08-03：车端首轮构建发现新增测试入口与 mock 状态缺失

- **现象**：Ubuntu 18.04/Melodic 构建新 `local_elastic_path_test` 时链接报
  `undefined reference to main`；QR 转向期间识别测试又在成功日志访问
  `qr_observation_numbers` 时抛出 `AttributeError`。导航和底盘均未启动。
- **原因**：新增 gtest 沿用了包内测试注册方式却遗漏独立 `main()`；以
  `object.__new__` 创建任务 mock 时只设置了检测字段，漏掉成功日志依赖的属性。
- **修复**：为 gtest 增加标准 `InitGoogleTest/RUN_ALL_TESTS` 入口；QR 用例补齐
  单元素 `qr_observation_numbers`。
- **预防**：新 test target 必须对照同包既有测试的入口形式；Python 任务 mock 触发
  成功/失败路径前，应完整检查该路径读取的所有实例字段。车端测试失败时禁止启动实车。

### 2026-08-03：真车测试停止脚本名与变更文档名误判

- **现象**：首次停止本轮真车任务时误调用不存在的 `stop_2026.sh`，幸而已先发布零速度，
  随后以精确 roslaunch 进程中断完成安全停机；复核本地变更记录时又读取了不存在的
  `2026-08-03-local-elastic-path.md`。
- **原因**：没有先通过文件清单核对仓库实际名称；正式停止脚本是
  `stop_2026_task.sh`，既有变更记录名为 `2026-08-03-add-local-elastic-path.md`。
- **修复**：确认并保留文档中已有的 `stop_2026_task.sh` 操作入口；已停止所有本轮车端
  launch、诊断 TF 节点及 WSL ROS Master，后续新增变更记录使用实际文件名。
- **预防**：任何停止、回滚或文档引用命令都先以 `rg --files` 或目录清单确认精确路径；
  紧急零速后优先调用仓库的精确停止脚本，不拼写推测的脚本名。

### 2026-08-03：本机 pytest 缺失运行依赖

- **现象**：尝试以本机 `python -m pytest` 运行生产任务几何测试时，pytest 导入阶段报
  `ModuleNotFoundError: No module named 'pluggy'`，测试没有开始执行。
- **原因**：Windows 本机 Python 3.13 的用户级 pytest 安装不完整；该环境也不是允许编译
  ROS Melodic/C++ 插件的车端 Ubuntu 18.04。
- **修复**：不在本机安装或修改依赖，改用标准库 `unittest` 运行可用的 25 项测试（14 项 ROS
  依赖用例按设计跳过）；C++/ROS 验收保留到车端 Catkin gtest。
- **预防**：本机运行 Python 回归前先检查测试运行器依赖；缺少依赖时优先使用项目已支持的
  标准库入口或目标平台 Catkin 测试，不把开发机依赖故障误判为业务测试失败。

### 2026-08-03：最大净空评分的空值初始化会掩盖所有候选

- **现象**：车端首次构建和 gtest 通过后，最终代码复核发现 `ElasticClearanceScore` 的无效默认
  `maximum_cost` 被设为 `255`；候选累积时使用 `max()`，会让每条实际安全候选仍显示为 255，
  因而不能低于 253/220 的接受阈值。该版本未启动 ROS 或车辆。
- **原因**：把“无效候选”的标识错误地编码到数值字段，而比较逻辑实际已经使用独立的 `valid`
  标志处理无效候选。
- **修复**：默认最高代价改为数值恒等元 `0`，并新增回归断言；无效状态只由 `valid=false`
  表示。修复后已重新部署并在车端重建测试。
- **预防**：对累计 `max/min` 的评分结构，构造默认值必须使用对应的数学恒等元；无效性与评分值
  必须由不同字段表示，并用单测同时覆盖初始状态和第一项累计。

### 2026-08-03：目标守卫半边长在错误的加载器作用域中定义

- **现象**：为消除固定 `0.25 m` 守卫边长而新增 `square_side_m / 2` 后，本机几何回归的
  `test_middle_target_guards_are_derived_from_adjacent_endpoints` 报 `NameError: half_side is not defined`。
  未上传小车、未启动 ROS 或发送运动命令。
- **原因**：补丁按相同的 `raw_points` 上下文落入通用 `load_numbered_points()`，而实际使用
  `half_side` 的 `load_middle_target_guard_points()` 没有该局部变量。
- **修复**：将读取、有限性和正值校验移动到守卫加载器；通用点加载器恢复为只校验点记录。
- **预防**：对同一文件中重复的补丁上下文，修改后立即在目标函数附近复读变量定义与全部引用；
  几何常量的回归测试必须在任何上传前重新运行。

### 2026-08-03：PowerShell here-string 给车端测试结果目录附加 CRLF

- **现象**：车端 `catkin_make --pkg ucar_2026` 与两个 nosetest 入口已成功，感知测试运行 7 项、
  任务测试运行 29 项且均为 `OK`；随后同一 here-string 中的 `catkin_test_results` 报
  `build/test_results/ucar_2026\r does not exist`。
- **原因**：Windows PowerShell here-string 以 CRLF 通过标准输入传给远端 Bash，最后一行的
  目录参数保留了 `\r`，导致汇总工具查询了不存在的带回车路径。
- **修复**：不依据该失败改动任何 ROS/任务代码；改用单行 SSH 命令独立读取真实测试目录的
  汇总结果。
- **预防**：PowerShell 向 Linux Bash 输送多行脚本时，避免让 here-string 的最后一个带路径参数
  命令承担关键成功判据；构建、测试与 `catkin_test_results` 保持独立命令，或先明确转换为 LF。

### 2026-08-03：到点 OCR 路线首轮实车在 `52→12` 的 move_base 失败关闭

- **现象**：从固定起点启动新路线后，安全门确认 `/odom_raw`、`odom → base_link`、
  `map → base_link` 和雷达均有限且新鲜；车辆成功到达 52（误差 `0.023 m`），相机仅在
  QR 阶段开启/关闭，读到去重后的 `a/d/i` 三个二维码。切换 `body_projection` 后，
  `52→12` 的 357 位姿全局路径被 `move_base` 接受，但约 56 秒后返回 status `4`；任务以
  `PRODUCTION_TARGET_012 failed with move_base status 4` 安全中止，尚未到达 12、未启动生产
  OCR 转圈，正式墙点为 0。
- **原因**：本轮日志只确认 action 失败码，尚未把具体碰撞/恢复拒绝原因归因到 CymPlanner、
  局部代价地图或定位；不能把 status `4` 误判为 OCR、相机或二维码故障。
- **修复**：任务立即关闭原生 OCR、发布 `ABORTED`，随后显式零速并调用
  `stop_2026_task.sh`；车端 launch 与 WSL Master 均已停止，未自动重跑或放宽 footprint/
  代价阈值。
- **预防**：下次实车前先针对 `52→12` 在 body-projection 模式做静态 global plan 与局部
  footprint/转向扫掠诊断，读取 move_base/CymPlanner 同时段日志后再决定是否改动路径或参数；
  status `4` 后必须由用户重新放回固定起点才可再运行。

### 2026-08-03：实车前探针混用了 PowerShell/WSL 参数和 Python 3 API

- **现象**：第一次以 `Start-Process wsl.exe` 启动 Master 时，`bash -lc` 的命令字符串被拆分，
  Master 未保留；随后探针又在 Melodic Python 2 中调用不存在的 `math.isfinite`。两次都发生在
  手动栈启动前或只读订阅阶段，没有下发非零速度。
- **原因**：把 WSL 的 `bash -lc` 最后一参数当作无需整体引用的 Windows 进程参数，并按 Python 3
  标准库假设编写了车端 Python 2 探针。
- **修复**：先用前台短时启动输出确认动态 Master 地址和 HTTP/1.0 兼容层，再以已验证的独立
  后台方式启动唯一 Master；有限性判断改为 `not math.isnan(x) and not math.isinf(x)`。随后安全门
  成功读取到有限 odom、两段 TF 和 247 个有效雷达样本。
- **预防**：Windows → WSL 长驻命令必须验证 `bash -lc` 的整个脚本参数确为一个 argv；车端临时
  ROS 探针一律以 Python 2/Melodic API 为准，先检查解释器能力再用于实车安全判据。

### 2026-08-03：实车结果文档的首个补丁假定了未折行的 Markdown

- **现象**：向到点 OCR 变更记录补写本轮实车 status `4` 结果时，首个 `apply_patch` 找不到
  “已知限制”段的一整行上下文而原子拒绝；没有改动任何文档、代码或运行状态。
- **原因**：补丁把原文件中已经换行的句子按未折行文本匹配。
- **修复**：先读取文件尾部的精确换行，再以相邻的短上下文补写实车验证与限制。
- **预防**：Markdown 长句补丁不要假定显示时的换行；追加运行结果前先读取目标段落原文。

### 2026-08-03：前视点回退的首轮车端验证误用启动器和上传目录

- **现象**：第一次通过 `run_melodic_python2.sh` 执行回归时，又把 `/usr/bin/python2` 当作脚本
  参数，解释器将 ELF 文件按 Python 文本读取而报语法错误。随后一次多文件 `scp` 的目标写成
  `~/ucar_ws/src/ucar_2026/`，三个文件被误放在包根目录，车端测试仍加载旧的 `scripts/` 与
  `test/` 文件；同轮本机静态搜索还因正则引号不完整而报未闭合分组。补充
  `resume_production_only` 顺序测试时，mock 又遗漏只用于 ROS 日志的 `camera_image_topic` 字段。
- **原因**：没有遵循项目 Python 2 包装器“首参数就是待执行脚本”的约定，也错误假定多源
  `scp` 会按本地子目录自动路由到远端；PowerShell 里嵌套正则的引号没有独立验证。
- **修复**：先以正确包装器参数得到预期红灯，再核验三个误放文件的精确绝对路径和哈希、只删除
  这三个本轮创建的副本；随后按 `scripts/`、`test/`、`launch/` 三个明确目标分别上传。车端
  构建成功，补齐 mock 后 32 项 Python 2 回归全部通过；全程未启动 ROS 或发送速度。最后一次
  哈希查询还因本机网络沙箱未授权 SSH 被拒绝，改为经已授权的 SSH 直接执行完整车端回归。
- **预防**：车端包装器调用前先读取其参数契约；多源上传必须分目标子目录并在运行测试前核对
  远端实际加载路径/哈希；用 `object.__new__` 构造的任务 mock 必须补齐被覆盖路径的日志字段；
  PowerShell 正则检查用独立短命令执行，不能与其他验收命令共享退出码；需要车端网络时，直接
  使用已批准的 SSH 通道，并以完整车端测试结果作为验收依据。

### 2026-08-03：本地 Git 暂存因 `.git/index.lock` 写权限受限失败

- **现象**：完成前视点回退的校验后，首次 `git add` / `git commit` 无法在仓库 `.git` 下创建
  `index.lock`，命令以权限拒绝结束，工作树内容和现有提交均未变化。
- **原因**：当前桌面会话的文件系统沙箱只授予工作树写权限，Git 元数据写入需要单独的受控授权。
- **修复**：不手动创建或删除锁文件；确认没有残留锁后，以受控 Git 写入权限重新执行同一组
  精确文件的暂存和提交。
- **预防**：在受限工作区完成校验后，如需创建本地提交，应直接使用范围限于 Git 写操作的授权，
  不得用删除 `.git/index.lock` 的方式绕过权限错误。

### 2026-08-03：点 12 的异步 OCR 候选丢失抓拍位姿

- **现象**：全程前视点实车试跑通过安全门，QR 即时读到 `a → d → i`，并成功从 52 到达 12
  （误差 `0.035 m`）。点 12 原地 OCR 转到 `3.850 rad` 时识别“食品加工车间”（`73.4`），
  任务报 `OCR candidate has no capture pose` 并安全中止，未继续至 23。
- **原因**：`start_async_motion_ocr()` 把 `capture_requested_pose_map` 存在 task 元数据；
  `finish_async_motion_ocr()` 却只返回 Python 3 helper 的 response，未把该元数据合并进去。
  `restore_ocr_capture_yaw()` 因而按安全设计拒绝使用没有位姿的候选。
- **修复**：`finish_async_motion_ocr()` 现复制 helper 的成功 response，并附加任务侧的
  `capture_requested_at` 和 `capture_requested_pose_map`；车端 Python 2 回归覆盖该交接。未因
  此修复启动 ROS 或小车，下一次物理试跑仍须经过完整安全门。
- **预防**：异步任务的响应契约必须同时包含 helper 识别结果和任务侧采样元数据；生产实车前，
  用带有效候选的 mock 覆盖 response 交接，而不是只覆盖空结果或 helper 异常。

### 2026-08-03：目标守卫诊断命令含有无效 PowerShell 管道

- **现象**：一次只读的静态地图守卫点核查命令含有不完整的 PowerShell 管道，命令在解析阶段
  失败；没有修改文件、连接 ROS 或发送车辆命令。
- **原因**：把多个探查步骤拼接为一条 PowerShell 管道时，末端缺少合法的命令段。
- **修复**：改为独立的短只读命令完成地图掩膜距离核查；18 个涉及的守卫端点到静态占据格的
  最近距离均不小于 `0.485 m`，不会落入 relay 的 `0.22 m` 静态掩膜。
- **预防**：PowerShell 的读取诊断也应先以短命令验证语法；对安全相关的地图结论，只记录实际
  成功执行的检查结果，不能把解析失败的命令当作证据。

### 2026-08-03：目标守卫首次审查遗漏严格连续性与行驶中断流

- **现象**：首次实现虽要求两个递增时间戳，但没有限制两帧的最大间隔；同时只在发 goal 前对
  扫描不可用失败关闭，行驶中 `/scan_global_obstacles` 断流或 TF 持续失败仍可能让目标继续运行。
  缺陷在最终只读审查中发现，尚未启动 ROS 或小车。
- **原因**：把“新鲜帧”和“连续证据”混为一层校验，且守卫设计只覆盖了预检而未贯穿导航监督
  生命周期。
- **修复**：相邻命中源时间戳间隔超过 `target_guard_scan_max_age` 会清空计数；导航期间持续检查
  最后一个成功 `map ← laser` 投影的接收时间，超时由监督循环执行取消、零速和停车确认后中止。
  新增长间隔、预检断流和行驶中断流的 Python 2 回归，车端 42 项全部通过。
- **预防**：涉及“连续传感器证据”的逻辑必须分别测试重复帧、倒序帧、长间隔、TF 失败夹帧和
  行驶期间断流；预检安全门与执行期间看门狗必须同时存在。

### 2026-08-03：守卫部署期间的 SSH 传输与 PowerShell 引用失败

- **现象**：首次多目录上传在 SSH banner 阶段超时；随后的远端哈希循环又被 PowerShell 提前
  展开 `$f`，使远端 Bash 得到不完整命令。没有启动 ROS、构建或发送车辆命令。
- **原因**：小车 Wi-Fi/SSH 当时出现间歇握手延迟，且把 Bash 循环变量放入 PowerShell 双引号
  命令字符串。
- **修复**：改用当前动态可达地址、绝对远端路径和分目录 `scp`，以远端 `grep` 核验最终源文件；
  在最后一次覆盖上传后才在 Ubuntu 18.04 上构建并运行回归。没有在小车保留备份目录或归档。
- **预防**：跨 PowerShell/SSH 的远端变量要用单引号脚本或避免循环；SSH 握手超时后先确认连接，
  分阶段上传并在构建前检查实际加载的关键源码标记，绝不把部分上传版本拿去启动或测试实车。

### 2026-08-03：目标守卫首轮实车的 OCR 墙点匹配不足

- **现象**：复位后通过有限 odom、两段 TF、三路约 12 Hz 扫描和 `/cmd_vel` 发布者安全门；
  12 在 429 守卫命中后正确取消，23 因共享 429 正确发目标前跳过，14/25/16 继续完成到点 OCR。
  三次都识别“日用品加工车间”，但均未匹配到有效墙点，任务以 `only 0/3 distinct OCR wall
  points were recognized` 安全中止。
- **原因**：本轮有效 OCR 框虽然存在，但居中与前向雷达投影没有落入
  `wall_reference_point_numbers` 的误差阈值；这与守卫点 429 的触发、里程计或 TF 安全门无关。
- **修复**：未在实车后擅自修改 OCR、雷达、地图或导航参数。任务异常路径发布零速并关闭相机/
  OCR，随后停止 `2026.launch` 和 WSL Master；完整原始结果保留在本轮
  `~/.ros/ucar_2026_observations/run_20260803_193632/`。
- **预防**：下一轮先离线检查该轮 OCR 框中心、抓拍位姿、前向距离与 wall-point 投影差，再决定
  是否调整 OCR 居中、雷达误差或墙点匹配；不能把识别文本正确误当作墙点定位成功。

### 2026-08-03：实车控制命令的 PowerShell 引号与死亡 ROS 注册

- **现象**：本轮两条只读/停止命令把 Bash 的 `$()` 放入 PowerShell 双引号，分别触发本机
  `ip`、`pgrep` 解析错误；车端停止脚本已发布零速并退出进程，但 Master 一度保留死亡节点注册。
  最终清理核验又错误假定 WSL 的 `ss` 位于 `/usr/sbin/ss`，该只读检查未执行。
- **原因**：PowerShell 不把反斜杠视为 `$()` 转义；ROS Master 对被强制终止的车端节点不会立即
  删除注册。
- **修复**：改用 PowerShell 单引号保存完整远端 Bash 脚本；先以 `ps` 证明车端 roslaunch、任务、
  move_base 和底盘进程均退出，再执行 `rosnode cleanup`，最后按已确认的 WSL Master PID 发送
  `SIGINT`。使用 `/bin/ps -p 20,68` 作最终 Master 核验；两端最终无残留启动进程。
- **预防**：Windows 到 Bash 的命令替换一律使用单引号变量传参，不使用 `\$()`；清理 Master
  注册前必须先确认目标节点端口/进程已经死亡，避免网络短暂抖动时误删仍在运行的节点。
  只读诊断工具也先用 `command -v` 确认路径，或使用已验证的 `ps` PID 检查。

### 2026-08-03：非交互 WSL 的 Master 地址自动发现阻塞

- **现象**：首次以短命令后台执行 `/home/car/start_ros_master.sh` 时，WSL 父进程退出后没有保留
  Master；随后隐藏长驻命令又在脚本调用 Windows PowerShell 自动发现地址处阻塞，11311 尚未监听。
  这发生在小车任务启动前，没有产生车辆命令。
- **原因**：非交互 WSL 到 Windows PowerShell 的地址发现不具备可依赖的返回时限，且短 WSL
  命令的子进程生命周期不等同于受 Windows 进程托管的长驻服务。
- **修复**：先读取当前物理 WLAN/WSL 地址 `192.168.8.198`，再按网络文档的一次性覆盖方式传入
  `ROS_IP` 与 `ROS_MASTER_URI`；从小车实际路由确认 11311 可达后才启动任务。
- **预防**：实车前先运行一次显式地址的 Master API/小车 TCP 检查；不要把短 `nohup` WSL 调用
  当作可验证的 ROS Master 后台守护方式。
