# 2026-07-14：调整默认目标至二维码扫描中心

## 目的

将默认导航目标的 X、Y 各减少 `0.20 m`，尝试将车辆停在二维码矩形更中心的位置；最终朝向设为起点 `yaw=0` 的反向。

## 涉及文件

- `ucar_ws/src/yolo2025/launch/2026.launch`
  - 首次中心点试探设为 `(-1.734, 1.905, 3.141593)`；实车观察 Y 偏低后，修正为 `(-1.734, 2.305, 3.141593)`。
  - 二维码旋转速度恢复为 `0.5 rad/s`。
- `docs/operations.md`
  - 更新默认目标与自动扫描行为的操作说明。

## 验证

- 本地 launch XML 校验通过。
- 小车端静态 launch 校验通过，且远端 SHA-256 与本地一致。
- 首次实车验证确认 Y 偏低；修正后的第二次实车验证已到达 `(-1.734, 2.305, π)`，并完成两圈二维码扫描，但仅识别到 `a`（`1/3`），随后安全停车。

## 已知限制

- 当前 Y 值是以 `0.20 m` 为校正量从原始值向上移动的试探；第二次测试仍未同时看到三个二维码，需根据识别日志继续调整目标点。
