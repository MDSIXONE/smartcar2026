# 2026-07-27 删除根目录 jie_ware 重复副本

## 目的

清理仓库根目录中与实车 Catkin 工作区重复的 `jie_ware/` 副本，避免维护时误改非正式路径，并在根 `README.md` 中明确该 ROS 包的唯一正式源码位置。

## 删除范围

- 删除根目录已跟踪的 `jie_ware/` 整个目录，共 10 个文件。
- 删除范围仅限 `jie_ware/.gitignore`、构建与包描述文件、许可证与说明文件、`launch/` 下的启动文件，以及 `src/` 下的源码文件。

## 保留的正式路径

- `ucar_ws/src/jie_ware/` 是 `jie_ware` 包唯一保留的正式源码路径。
- 该路径下原有 10 个已跟踪文件全部保留，内容未修改。

## 验证结果

- 已确认仓库根目录 `jie_ware/` 不存在。
- 已确认 `ucar_ws/src/jie_ware/` 仍有 10 个已跟踪文件。
- 已确认变更前后正式路径的 10 个索引对象哈希清单一致，并通过聚焦 diff 确认该路径未产生修改。
- 已执行 `git status --short` 与聚焦 diff，确认本次整理仅涉及根目录重复副本删除、根 `README.md` 目录说明更新及本文档新增；工作树中其他既有改动未被触碰。
- 已重新查看 `犯错档案.md`；当前仅有模板，无历史错误条目需要追加检查项。

## 已知限制

- 本次仅整理仓库文件，不执行构建或运行测试。
- 本次不改变任何构建、启动、部署或回滚命令，因此未修改 `docs/operations.md`。
- 工作树在本次整理前已有其他未提交改动，本次未处理、覆盖或纳入这些改动。
