# 2026-07-14：小幅提高 CymPlanner 线速度 P 增益

## 目的

在已确认导航链路和 `1.0 m/s` 命令限速生效后，小幅提高线速度比例增益，使车辆在仍受安全速度上限约束的前提下更快达到目标行进速度。

## 涉及文件

- `ucar_ws/src/cym_planner/config/ucar_cym_planner_params.yaml`
  - `linear_x_gain` 从 `4.0` 调整为 `6.0`。
- `docs/operations.md`
  - 更新当前 P/D 参数记录。

## 验证

- 本地 YAML 解析已通过：P=`6.0`、D=`0.2`、线速度上限=`1.0`。
- 已上传小车并完成 SHA-256 校验：`d84d5cd5…7bda960c`。
- 当前导航任务已停止；本次未启动 ROS 或车辆。

## 已知限制

- `max_vel_x` 仍为 `1.0 m/s`，因此 P 增大只会使速度更快达到上限，不会提高该上限。
- 若出现接近目标时的抖动，应降低 P 或增大 D，而不是继续提高速度上限。
