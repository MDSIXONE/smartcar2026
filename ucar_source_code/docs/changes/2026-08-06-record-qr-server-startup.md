# QR 服务器 SSH 登录与启动方法记录

## 目的

记录二维码接口服务器（`192.168.8.1:3663`）的 SSH 登录方式与启动命令，避免再次
遗忘服务器侧启动方法。

## 涉及文件

- `docs/operations.md`（新增「QR 服务器（192.168.8.1）」小节）

## 内容

- SSH 登录：`ssh -l root 192.168.8.1`，密码 `051520zyfZYF`。
- 启动：登录后执行 `python3 /root/server.py`。
- 服务器为局域网网关 `192.168.8.1` 上的独立 HTTP 服务（端口 `3663`），不依赖 ROS，
  也不是 ROS Master；服务未启动时仅 `/qr_api_result` 查询失败，`/qr_result` 不受影响。

## 验证结果

- 文档更新后未涉及代码或启动命令的实质变更；`docs/operations.md` 中新增小节已与
  已有「接口查询目标默认为 `http://192.168.8.1:3663`」描述保持一致。

## 已知限制

- 密码为明文记录，仅限本团队局域网设备使用；若服务器密码变更需同步更新本文档。
