# OCR 内墙 40cm 角度扫读二次确认

## 行为

首次 OCR 识别并完成雷达/地图墙点定位后，主流程使用 `ocr_stop_offset_m=0.40` 到达墙内 40cm 停车点。
车辆以墙面法向为基准，先转到 `+45°`，再以受控角速度向 `-45°` 连续旋转；旋转过程持续抓取 OCR 帧。
识别到与首次类别一致的结果后停止扫读并保留二次识别记录；完整范围内都未确认则任务报错，不继续播报或放置。

## 记录与验证

- 观察记录增加 `processing_stop_rechecks` 和 `processing_stop_recheck`，包含图像、文字、置信度、角度和类别。
- 三套 launch 的停车内缩参数统一为 `0.40m`。
- 本地验证：三套感知测试均 `14/14`；几何测试 `106/106`、`122/122`、`107/107`，ROS 环境分别跳过 `89`、`105`、`90` 项；三套 Python AST、三份 launch XML 和 `git diff --check` 通过。
- 部署：已确认动态主机名 `ucar-mini` 解析到车端 `192.168.8.231`，同步前车端无 2026 主流程、`move_base`、`roscore` 或 `lidar_loc` 运行；同步后需逐文件核对 SHA-256，未自动启动任务。
