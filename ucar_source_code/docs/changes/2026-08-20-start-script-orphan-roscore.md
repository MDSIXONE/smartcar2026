# 2026-08-20 启动脚本清理孤儿 ROS Master

## 目的

处理上一轮启动脚本异常退出后，车端只剩一个孤立 `roscore`，导致下一次
`start_2026.sh` 因检测到已有 ROS Master 而无法启动的问题。

## 改动

- 启动前动态匹配 PPID 为 1、命令为车端 Melodic `roscore` 的孤儿进程。
- 只有 ROS 节点列表严格为 `/rosout` 时才发送 `SIGINT`；存在其他节点则拒绝自动清理。
- 不写死 `9014` 等 PID，保留已有活动 Master 拒绝启动保护。

## 验证与上线

- 本地和车端 `bash -n` 通过。
- 车端 `check` 模式已实际清理 PID 9014，启动自管 Master 后正常退出；未启动导航、未发送运动指令。
- 启动脚本已同步到 `ucar-mini`，本地/车端 SHA-256 一致。
