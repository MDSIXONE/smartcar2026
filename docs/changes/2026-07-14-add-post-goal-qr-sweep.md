# 2026-07-14：CymPlanner 定角二维码扫描

## 目的

让导航初始目标直接使用顺时针 90° 的朝向（ROS `yaw=-π/2`），到点即对准二维码 `i`。随后通过 move_base/CymPlanner 向同一坐标依次发送 180°、270° 的朝向目标，静止扫描 `a`、`d`，最后停在 270°。

## 涉及文件

- `ucar_ws/src/yolo2025/scripts/2026.py`
  - 删除直接 `/cmd_vel` 旋转；二维码定角转向改为 move_base action 目标，确保与 CymPlanner 目标一致。
- `ucar_ws/src/yolo2025/launch/2026.launch`
  - 默认目标 yaw 改为 `-1.570796`，即顺时针 90°；保留二维码停留时长和超时参数。
- `docs/operations.md`
  - 更新目标朝向与二维码扫描顺序。

## 验证

- 本地 Python 2 语法和 launch XML 校验通过。
- 小车端 Python 2 语法校验和静态 launch 检查通过。
- 未同步或修改全局代价地图、CymPlanner/PD 配置或其他导航配置。

## 已知限制

- 此版本等待实车验证 move_base 对同点朝向目标的到达顺序与二维码识别结果。
- 每个二维码朝向停留 4 秒，整个二维码阶段超时为 40 秒。

## 2026-07-14 实车启动验证

- 已将本版本部署到小车并启动 `yolo2025 2026.launch`，随后安全停止。
- 验证未进入导航：底盘持续发布 `wheelodom` 的 NaN 位姿，TF 拒绝该变换；导航节点在 15 秒内未等到 `move_base` action server。
- 因此本次没有发送任何导航目标，也没有开始二维码转向/扫描；待里程计和 `move_base` 正常后，需重新执行实车验证。

## 2026-07-14 实车复验结果

- 复验时里程计和 `move_base` 均正常；默认导航目标已到达：`(-1.734, 2.305, -1.570796)`。
- 到点的 `i` 扫描、CymPlanner 的 `yaw=pi`（a）目标、CymPlanner 的 `yaw=pi/2`（d）目标均成功；终端记录的识别顺序为 `i`、`a`、`d`。
- 二维码阶段完成时日志为 `codes=3/3`，最终朝向为顺时针 270°（ROS `yaw=pi/2`）。

## 2026-07-14 扫描顺序调整

- 按现场要求，将扫描顺序改为 `d → a → i`：默认导航目标改为顺时针 270°（d），随后由 move_base/CymPlanner 对准 180°（a）和 90°（i）。
- 最终停留朝向改为顺时针 90°（ROS `yaw=-pi/2`）。本次仅完成代码和静态校验，尚未按新顺序进行实车复验。

## 2026-07-15 停留时间调整

- `qr_scan_hold_sec` 从 `4.0` 秒调整为 `0.5` 秒；三次定向扫描均使用此时长。
- 已完成 launch XML 静态校验并同步到小车，未启动实车任务。
- 0.5 秒对摄像头曝光和二维码解码的余量较小；若出现漏识别，可单独调高该参数。
